NEXT_PUBLIC_CONVEX_URL=https://aware-buzzard-478.convex.cloud
OPENAI_MODEL=gpt-4o-mini

# Use only where the OpenAI call runs.
# If Next.js route handlers run the AI conversion on Netlify, set this in Netlify.
# If Convex actions later run the AI conversion, set this in Convex instead.
OPENAI_API_KEY=
